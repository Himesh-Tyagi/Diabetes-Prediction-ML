# Diabetes Prediction Project

This is a Machine Learning project to predict whether a person has diabetes using health data.

### 📍 Project Overview

This project uses the Diabetes Dataset to train a machine learning model that predicts if a person is diabetic based on various health measurements.

---

## 🧠 Features

✔️ Exploratory Data Analysis (EDA)  
✔️ Data cleaning and preprocessing  
✔️ ML model training  
✔️ Model saved as `diabetes_model.pkl`  
✔️ Web app (Flask / Streamlit) to interact with the model  
✔️ Requirements for running the app

---

## 📦 Files In This Repo

| File | Description |
|------|-------------|
| `diabetes.csv` | Dataset used for training |
| `diabetes INTERN.ipynb` | Notebook with full model training |
| `diabetes_model.pkl` | Trained model file |
| `sc.pkl` | Scaler used for preprocessing |
| `diabetesapp.py` | Python app to run the prediction |
| `requirements.txt` | Required libraries |

---

git clone https://github.com/Himesh-Tyagi/Diabetes.git
cd Diabetes
